# 机构（达人代运营）板块专项审查

**范围**：10 个页面（5218 行）+ 22 个 API 路由（1724 行）+ 12 张表
**日期**：2026-07-25
**结论**：找到 **18 个问题**，已修复 **13 个**（含全部 P0）。

> ## ✅ 修复状态（2026-07-25）
> `tsc` 零错误，5 项流程验证全过。业务规则按你的选择实现：
> **阶段必须按顺序（管理员可 force 跳级）· 倒退保留历史 · 各阶段完成对应各自的状态 · 一个达人只能有一份生效合同**
>
> **已补集成测试**：`npm run test:flows`（20 项，全过）。覆盖达人流程、评级口径、
> 订单链路、VAT 月度、时区边界、枚举一致性——就是这几轮 bug 最密集的地方。
> 每次改完跑 `npm run verify` 可以一次跑完枚举检查 + 链路测试 + 类型检查。

---

## 🔴 P0 — 数据/统计直接是错的

### A1. 「A级达人」统计永远是 0，A级评估加分永远不发

系统里有两个评级字段：老的 `rating` 和新的 `final_rating`。新评估接口只写 `final_rating`，`rating` 全是空字符串。而两处统计各用了一个：

| 位置 | 查询条件 | 实际结果 |
|---|---|---|
| 机构看板「A级达人」`agency/stats:13` | `WHERE ie.rating = 'A'` | **0**（rating 列已废弃，永远空） |
| 积分引擎 A级评估加分 `internal/points:373` | `WHERE final_rating = 'A'` | **0**（A 级实际都是 `A+`） |
| 真实 A 级数量（含 A+） | `final_rating LIKE 'A%'` | **10** |

实测数据：16 条评估里 10 条是 `A+`、2 条 `B+`、2 条 `B`、2 条 `C`，**没有任何一条是纯 `A`**。因为评分算法在直播占比 ≥50% 时会给等级加 `+`，而 A 级达人基本都满足这个条件。

所以：看板上 A 级达人永远显示 0；做评估的人永远拿不到那 5 分/个的加分。

**改法**：统一用 `final_rating`，比较改成 `LIKE 'A%'` 或先 `REPLACE(final_rating,'+','')`。老的 `rating` 列建议废弃并从 `agency/stats` 移除。

---

### A2. 步骤更新接口不校验归属，可改任意达人的任意步骤

`src/app/api/influencers/[id]/steps/route.ts`

```ts
export async function PATCH(req: NextRequest) {     // ← 注意：没有接 params
  const { step_id, status, ... } = body;
  db.prepare(`UPDATE influencer_steps SET ... WHERE id = ?`).run(...vals, step_id);
```

URL 里的 `[id]`（达人 ID）**从头到尾没被用过**。任何登录用户 `PATCH /api/influencers/1/steps` 带上别人的 `step_id`，就能改任意达人的任意步骤——包括触发阶段流转、把别人的达人置为「已停止」。

同时 `status` 没有白名单，而 `influencer_steps.status` 在数据库里**也没有 CHECK 约束**，所以任何字符串都会被写进去。写进去之后：进度统计错乱、阶段流转判断失效（判断依赖精确匹配"已完成"）。

**改法**：接 `params`，`WHERE id = ? AND influencer_id = ?`；status 走 `enums.ts` 白名单；给 `influencer_steps.status` 补 CHECK 约束。

---

### A3. 阶段完成后状态被无脑写成「已入池」

`influencers/[id]/steps/route.ts` 的自动流转：

```ts
db.prepare("UPDATE influencers SET phase = ?, status = '已入池' ...")
```

不管完成的是哪个阶段，状态一律改成「已入池」：

| 完成的阶段 | 应该是 | 实际变成 |
|---|---|---|
| 发现（discovery） | 已入池 ✅ | 已入池 |
| 签约（contract） | **已签约** | 已入池 ❌ |
| 孵化（incubation） | **已完成** | 已入池 ❌ |

线上数据已经有 2 个 `已入池 / completed_contract` 的达人——他们其实是签约流程走完的，却被标成"回池子里了"。业务含义完全相反。

---

### A4. 发现任务重复点「完成」会让步骤翻倍

`discovery-tasks/route.ts` PATCH 的 `completed` 分支没有任何幂等保护——不检查任务当前状态，直接重新 `seedInfluencerSteps()`。

实测：

```
第一次完成后 5 步 → 再点一次变成 10 步   （5 种步骤各 2 份）
```

前端如果重复提交（网络慢时用户多点一次），达人的步骤列表就会出现两套。而且这段没有事务，中途失败会留半套。

---

## 🟠 P1 — 状态机与流程漏洞

### A5. 阶段可以随意跳级、倒退

`start-phase` 只检查"不能重复启动当前阶段"，不检查前置阶段是否完成。所以：

- 一个刚创建、评估都没做的达人，可以直接 `POST {phase:"incubation"}` 跳到品牌孵化
- 已经在孵化阶段的达人，可以倒退回 `contract`——**而倒退会先 DELETE 掉该阶段所有步骤和备注**，签约阶段的历史记录直接没了，没有任何二次确认

这也解释了线上那几条矛盾数据：`已签约 / discovery`、`不推荐 / contract`。

### A6. 孵化列表混进了不该出现的人

`/api/influencers?phase=incubation` 的过滤条件是：

```sql
phase IN ('completed_discovery','contract','completed_contract','incubation','completed_incubation')
```

意图大概是"走到过这个阶段的人"，但没排除终止状态。实测孵化页面（品牌孵化）里出现：

| 状态 | 人数 | |
|---|---|---|
| 签约中 | 3 | |
| 已签约 | 2 | |
| 已入池 | 2 | |
| **不签约** | **2** | ❌ 已经谈崩了还在孵化列表 |
| 品牌孵化中 | 1 | |
| **不推荐** | **1** | ❌ 评估就没通过 |

`phase=contract` 同理。

### A7. 合同可以重复创建，每建一次都重写达人状态

`contracts` POST 不检查该达人是否已有合同。实测同一个达人建了 3 份合同，每次都执行 `UPDATE influencers SET status='已签约'`——即使这个达人当前是「已停止」也会被拉回「已签约」。

### A8. 「已停止」这条路走不通

步骤接口里有处理 `status === "已停止"` 的分支（把达人整体置为已停止），前端也画了对应的样式。但前端**没有任何按钮**能发出这个状态——`handleStepUpdate` 只在"标记完成"和"标记阻塞"时被调用。所以停止一个达人的功能实际不存在，那段代码是死的。

反过来，前端会发 `阻塞` 状态，但后端对 `阻塞` 没有任何处理——达人主状态不变，看板也不体现。

### A9. 新建达人绕过发现任务流程

「新建达人」页面直接 POST，不带 `discovery_task_id`，接口默认给 `status='待评估'` 并立即生成 5 个发现步骤。而发现任务流程期望的是：达人先是 `待提交`，任务完成时才批量转 `待评估` 并生成步骤。

两个入口进来的达人状态起点不一致（实测：直接新建的 2 个达人，一个是 待评估、一个已经是 已签约）。如果之后这个达人又被挂到某个发现任务上，任务完成时会再 seed 一次 → 触发 A4 的步骤翻倍。

---

## 🟡 P2 — 校验与一致性

### A10. 评分档位是硬编码字符串，传错一个字静默算 0 分

```ts
const GMV_TIER_SCORES = { ">30万": 29, "20-30万": 25, ... };
const gmv_score = GMV_TIER_SCORES[gmv_tier] || 0;      // ← 不认识就 0 分，不报错
```

四个维度都是这个写法，且**专业度档位用的是泰文键**（`"เยี่ยมมาก"`、`"ดีมาก"`…）而其他三个是中文。前端只要传了不在表里的值（多个空格、换了措辞、泰文输错），该维度直接 0 分，最终评级从 A 掉到 C，**用户完全不知道发生了什么**。

### A11. 评级阈值可能过松

满分是 29+14+14+5 = **62 分**，而 A 级门槛是 50 分（80.6%），B 级门槛 20 分（32%）。实测 16 条评估里 10 条满分 62 分——档位选最高就必然满分，区分度存疑。这条需要你确认业务预期，不一定是 bug。

### A12. 发现任务 status 无白名单

`discovery_tasks.status` 有 CHECK 约束 `('active','completed')`，但 PATCH 的 `else if (status)` 分支直接写入任意值 → CHECK 报错 → 500。

### A13. 合同接口不校验达人是否存在

`influencer_id` 只靠数据库外键兜底，传一个不存在的 id 会抛外键异常变成 500，而不是友好的 404。

### A14. 达人 PATCH 不校验状态值

`influencers.status` / `reply_status` / `phase` 都有 CHECK 约束，但 PATCH 只做了字段名白名单（防注入），没做**值**白名单。传个不认识的状态 → 500。

### A15. 评估记录的 `evaluated_by` 前端还在传

接口已改成用 `auth.name`（正确），但前端还在 body 里传 `evaluated_by`，容易让人误以为可以指定。

---

## 🔵 P3 — 体验问题

### A16. 步骤序号按阶段各自从 1 开始，列表里两个阶段交错显示

`seedInfluencerSteps` 每个阶段的 `step_order` 都从 1 开始，而查询是 `ORDER BY step_order`。实测达人 #4 的显示顺序：

```
step_order=1  [discovery] 刷 TikTok 直播找达人
step_order=1  [contract ] 联系达人沟通报价方案     ← 交错
step_order=2  [discovery] 无合适人选则 Facebook 发帖
step_order=2  [contract ] 老板审批后开具发票       ← 交错
...
```

详情页靠 `currentPhaseSteps` 兜住了大部分场景，但只要展示"全部步骤"就是乱的。

**改法**：`ORDER BY CASE phase WHEN 'discovery' THEN 1 WHEN 'contract' THEN 2 ELSE 3 END, step_order`，或让 step_order 全局连续。

### A17. 达人列表的评级筛选只按首字母

```ts
(i.latest_rating || "").replace("+", "") !== ratingFilter
```

把 `A+` 归到 `A`——这个处理是对的。但列表展示的 `latest_rating` 取的是**最新一条评估**，而看板统计用的是**去重达人数**，两处口径不同。

### A18. 孵化页只查了工厂和达人，没有孵化进度

`agency/incubation` 页面只调 `/api/factories` 和 `/api/influencers?phase=incubation`，没有读 `influencer_steps` 的孵化阶段进度。所以"品牌孵化"页面看不到孵化到哪一步了。

---

## 建议修复顺序

**第一批（数据正确性，建议今天修）**
1. A1 评级统计口径（改两个查询，5 分钟）
2. A2 步骤接口归属校验（安全洞）
3. A3 阶段完成后的状态映射
4. A4 发现任务幂等保护

**第二批（状态机，需要你先确认业务规则）**
5. A5 阶段流转的前置条件——**需要你回答**：允不允许跳级？倒退要不要保留历史步骤？
6. A6 孵化/签约列表要不要排除 不推荐/不签约/已停止？
7. A7 一个达人能有几份合同？续签算新合同还是改旧的？
8. A8 「停止合作」要不要做成正式功能？

**第三批（校验补齐）**：A10、A12、A13、A14 —— 机械工作，可批量处理

**第四批（体验）**：A16 排序、A18 孵化进度

---

## 需要你确认的业务规则

第二批的修复取决于这几个问题，麻烦告诉我：

1. **阶段能不能跳**？比如特别优质的达人，能不能跳过签约直接进孵化？
2. **阶段能不能退**？退回时之前的步骤记录是保留还是清空？（现在是直接删掉）
3. **完成签约阶段后**，达人状态应该是「已签约」还是别的？完成孵化后呢？
4. **一个达人能有几份合同**？续签是新建一份还是改原来那份？
5. **「不推荐」「不签约」的达人**，还需要出现在后续阶段的列表里吗？


---

# 修复记录（2026-07-25）

## 已修（13 项）

| # | 问题 | 改法 |
|---|---|---|
| A1 | A级统计恒为 0 | `agency/stats` 改用 `final_rating LIKE 'A%'`（原来查废弃的 `rating` 列）；积分引擎同样从 `= 'A'` 改成 `LIKE 'A%'`，不再漏掉 A+ |
| A2 | 步骤接口不校验归属 | PATCH 接上 `params`，`WHERE id = ? AND influencer_id = ?`；status 走白名单（新增 `INFLUENCER_STEP_STATUSES`）；补 client 拦截；步骤更新 + 阶段流转包进事务 |
| A3 | 阶段完成一律写「已入池」 | 按阶段映射：发现→已入池、签约→**已签约**、孵化→**已完成**。同时写了一次性数据订正，启动时自动修正存量记录（实测订正 2 条） |
| A4 | 发现任务重复完成步骤翻倍 | 先查当前状态，已完成直接原样返回；每个达人先查有没有发现步骤再决定是否 seed；整段包进事务 |
| A5 | 阶段可随意跳级 | 按 `discovery → contract → incubation` 校验前置阶段是否全部完成；**管理员可带 `force: true` 强制跳**，跳级会写进审计日志 |
| A6 | 倒退会删掉历史 | 改为：该阶段已有步骤就沿用，只有从没生成过才 seed。之前做过的记录和备注都保留 |
| A7 | 合同可重复创建 | 已有生效合同时返回 409 并提示「续签请直接修改该合同」；另外已停止合作的达人不再被建合同悄悄拉回「已签约」 |
| A10 | 评分档位传错静默算 0 分 | 四个维度的档位值都做白名单校验，不认识的值返回 400 并列出可选项（核对过前后端档位完全一致，不会误伤） |
| A12 | 发现任务 status 无白名单 | 接入 `validateEnums` |
| A13 | 合同不校验达人是否存在 | 补 404 |
| A14 | 达人 PATCH 不校验状态值 | 两个入口（`/influencers` 和 `/influencers/[id]`）都接入 `validateEnums`，覆盖 status / reply_status / phase |
| A15 | 前端还在传 evaluated_by | 去掉，改由后端取登录态；顺带让保存失败时显示后端返回的具体原因 |
| A16 | 步骤按阶段交错显示 | 三处查询都改成 `ORDER BY CASE phase ... , step_order` |

另外修了两个扫描时没单独列出的：
- 发现任务完成时**无条件**把达人状态改写成「待评估」，会把已签约/已停止的达人打回去。现在只推进「待提交」的。
- 已停止合作的达人现在不能再开启新阶段。

## 未修（5 项，都有原因）

| # | 问题 | 为什么没动 |
|---|---|---|
| A6-b | 孵化/签约列表混入不推荐、不签约的达人 | 你在选项里没勾这条。要改的话改一行 SQL（加 `AND status NOT IN ('不推荐','不签约','已停止')`），随时可以加 |
| A8 | 「停止合作」前端没有入口 | 同上，没勾。后端逻辑已经能用了（且这次补了状态白名单，`已停止` 是合法值），只差前端一个按钮 |
| A9 | 新建达人绕过发现任务流程 | 属于产品设计问题——"直接新建达人"和"通过发现任务录入"本来就是两个入口。这次已经消除了它的副作用（重复 seed 步骤），保留两个入口本身没问题 |
| A11 | 评级阈值可能过松 | 满分 62，A 门槛 50。实测 16 条评估里 10 条满分——档位选最高就必然满分。这是业务参数不是 bug，要调告诉我数值 |
| A17/A18 | 列表评级口径、孵化页无进度 | 体验优化，不影响正确性 |

## 验证

- `npx tsc --noEmit` → 0 error
- 流程验证 5/5：A级统计从 0 变 10、阶段完成状态映射正确、幂等保护生效、无重复生效合同、步骤排序按阶段分组
- 存量数据订正演练：`completed_contract + 已入池` 2 条 → 已签约

## 部署提醒

启动时会自动订正存量的阶段完成状态（控制台会打印订正条数）。改动前建议先备份 `data.db`。
