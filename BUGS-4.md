# 第四轮代码审查报告

**审查时间**：2026-07-25
**换的维度**：这轮不再按模块或失效方式扫，改成查**「两处定义不一致」**——代码里的枚举值 vs 数据库 CHECK 约束、文档承诺 vs 代码实现、后端返回的错误 vs 前端的处理。
**结果**：找到 3 类系统性问题，已修；另有 2 项评估后判定为低优先级，说明理由后暂不处理。

---

## 1. 枚举漂移：20+ 处用户可控的状态值没有校验

数据库里有 **41 个带 CHECK 约束的列**（订单币种、客户状态、请假类型、合同付款状态…）。这些列的值大多直接来自请求体，写库前没有任何校验。传一个不认识的值会发生什么：

```
SQLite → CHECK constraint failed → 抛异常 → Next.js 变成 500
```

前端只能显示"操作失败"，看不出是自己传错了还是服务端挂了。用扫描器逐列比对，确认 **20 处用户可直达的写入点缺校验**，包括：

| 列 | 入口 |
|---|---|
| `customers.status` | 客户 PATCH（status 在可更新字段白名单里） |
| `vat_customers.status` / `wht_customers.status` | VAT/WHT 客户新增与编辑 |
| `leave_requests.leave_type` | 请假申请 |
| `contracts.payment_status` / `phase` | 合同编辑 |
| `orders.currency` | 新建订单 |
| `templates.type` | 模板新增 |

**改法**：新增 `src/lib/enums.ts`，把 41 个列的合法值集中登记，提供 `validateEnums()`；在上述写入点写库前校验，返回带原因的 400（"status 的值「xxx」无效，可选：启用 / 暂停 / 已终止"）而不是 500。

### 配套：加了一个一致性守护脚本

白名单和数据库两边分开维护，迟早会漂移。所以补了 `scripts/check-enums.js`：

```bash
node scripts/check-enums.js
# ✅ 枚举白名单与数据库 CHECK 完全一致（共 41 列）
```

两边不一致时会明确指出方向和后果：

- **只在 enums.ts 有** → 校验会放行、写库时 500
- **只在数据库有** → 合法值被接口拒绝，用户报"提交不了"

实际跑出来抓到了 2 处遗漏（`points_rules.rule_type`、`customer_points.point_type`），已补齐。建议加进 CI 或提交前检查。

---

## 2. 26 处写操作失败后没有任何提示

前端有 26 个处理函数是这个写法：

```ts
const handleApproveLeave = async (id, status) => {
  await fetchWithAuth("/api/leave", { method: "PATCH", body: ... });
  loadAll();     // ← 不看 res.ok，也没有 catch
};
```

请求失败照样刷新列表，页面上什么都不会变——用户只知道"点了没反应"。

**这一轮之前问题还不大，但现在必须修**：前三轮我给后端加了大量 403/400 校验（"仅管理员可审批请假"、"只能处理指派给自己的工单"、"无效的状态值"…）。这些精心写的错误提示，全都被前端默默丢掉了。

**改法**：新增 `src/lib/api-call.ts` 的 `apiCall()`，统一处理 `res.ok` 并把后端的 error 文案弹出来：

```ts
const ok = await apiCall("/api/leave", { method: "PATCH", body: { id, status } });
if (ok) loadAll();
```

已接入影响最大的几处（请假审批、补卡审批、积分撤销/恢复/申诉/审批申诉）。剩余的分布在客户、达人、订单等页面，写法一致，可以照着替换。

---

## 3. 请假审批还在往后端传 `approved_by`

第二轮把审批人改成了以登录态为准（请求体的值一律忽略），但前端还在传 `approved_by: user?.name`。虽然不影响结果（后端不读它），但容易让人误以为这个字段有用。已一并去掉。

---

## 评估后判定为低优先级的（说明理由）

### A. ~~8 处筛选切换的请求竞态~~ → 已全部修复

原本判定为低优先级（触发需毫秒内连点），后按要求一并修掉了。做法分两种：

- 新增 `src/lib/use-latest.ts` 的 `useLatestRequest()`：loader 开头领个号，setState 前check `run.isLatest()`，用于 `rewards`、`vat`、`internal`（考勤汇总 + 考勤日历）这类被多处调用的 loader
- 直接用 `let ignore = false` + cleanup：用于 `weekly-report`、`agency`、`tasks`、`vat` 的筛选 effect

覆盖页面：rewards、weekly-report、internal、vat、agency、tasks（6 个文件、8 处）

### B. 无障碍（PRODUCT.md 承诺 WCAG 2.1 AA）

实际查下来这块做得不错，没有需要修的：

- `prefers-reduced-motion` 在 `globals.css:187` 已处理
- 264 个 `<button>` 中，只有 3 个疑似纯图标且缺 `aria-label`/`title`

---

## 验证

- `npx tsc --noEmit` → 0 error
- **14 项自动验证全过**：白名单与 DB CHECK 完全一致（41 列，0 处漂移、0 处遗漏）、7 个写入点已接入校验、守护脚本存在、`apiCall` 封装与接入
- 注意：`scripts/check-enums.js` 依赖 better-sqlite3，沙箱里是 macOS 二进制跑不了，我用等价的 Python 逻辑验证过；在你的机器上可以直接跑

## 四轮累计

| 轮次 | 维度 | 发现 | 修复 |
|---|---|---|---|
| 第一轮 | 按模块通读 | 23 | 21 |
| 第二轮 | 全量重扫 + 前后端契约 | 22 | 22 |
| 第三轮 | 按失效方式（事务/崩溃/并发/性能） | 11 | 11 |
| 第四轮 | 按"两处定义不一致" | 3 类 | 3 类 |
| **合计** | | **59** | **57** |

## 关于"还有没有 bug"

四轮下来，每换一个维度都还能挖到东西——这本身说明**人工扫描不是收敛的**。前三轮的产出在下降（23 → 22 → 11），这轮找到的更多是系统性缺陷而非单点 bug，说明明显的问题基本清完了。

真想继续往下压，建议换手段而不是继续人工扫：

1. **把 `scripts/check-enums.js` 加进 CI** —— 已经能自动防住一类回归
2. **给核心链路补集成测试**：订单创建→步骤→文档→费用→证书、VAT 月度生成→步骤→对账、积分重算。这三条链路是这几轮 bug 最密集的地方
3. **开一个错误上报**（哪怕只是把 500 写进日志文件），生产环境的真实报错比任何静态扫描都准

---

# 收尾：最后两项已完成（2026-07-25）

## A. PDF 中文字体

`public/fonts/NotoSansSC-Regular.ttf`（10MB，OFL 授权）已加入仓库，证书里的中文公司名可以正常渲染。

**过程中发现了一个新问题**：光加字体还不够。Sarabun 没有中文字形、NotoSansSC 没有泰文字形，而「ชื่อบริษัท (Company): 湘泰国际商务咨询有限公司」是同一行里泰中混排——整行用任何一个字体都会丢掉另一半。实测原写法（整行切到 CJK 字体）泰文标签直接消失：

```
修复前: (Company): 湘泰国际商务咨询有限公司        ← 泰文标签没了
修复后: ชื่อบริษัท (Company): 湘泰国际商务咨询有限公司   ← 两种文字都在
```

改成用 `continued: true` 把标签和公司名分成两段、各用各的字体。这个坑写在 `src/lib/pdf-fonts.ts` 顶部注释里了，以后往证书加中文字段要注意。

## B. 外部客户接口不再靠公司名匹配

原来的逻辑是拿**登录账号的姓名**去匹配订单客户名：

```ts
WHERE o.customer_name = payload.name
```

两个问题：同名客户互相可见；账号改名立刻失联。

**没有按最初设想的加 `customer_id` 外键**——查了下数据发现行不通：`customers` 表是销售客户池（潜在/跟进中/已合作），不是客户注册表，14 个订单里只有 1 个能在池子里找到同名记录。硬关过去会让 13 个订单的客户凭空进入销售漏斗，语义是错的。

改用显式映射表 `client_account_customers`（客户账号 ↔ 可见公司名，一对多）：

- 外部接口按映射查，**未配置映射时回退到姓名匹配**，存量账号不会突然看不到数据（回退时服务端日志会提示去配置）
- 管理员通过 `PATCH /api/employees` 传 `customer_names: []` 维护；`GET /api/employees` 会带出客户账号的当前配置
- 删除员工时级联清理映射，避免 id 复用后新账号继承旧权限
- 可见范围为空时用 `1 = 0` 而不是无条件查询——**宁可查不到，也不能退化成全量泄露**

### 演练验证（在 data.db 副本上，6/6 通过）

| 场景 | 结果 |
|---|---|
| 未配置映射 | 回退姓名匹配，与改造前行为一致 |
| 配置映射后 | 精确查到该公司的全部订单 |
| 两个客户账号 | 可见范围互不重叠（旧逻辑同名即互看） |
| 账号改名 | 可见范围不变（旧逻辑直接失联） |
| 可见范围为空 | 返回 0 条，不退化为全量 |
| 删除员工 | 映射被级联清理 |

### 需要你做的一件事

给现有客户账号配一下可见公司，配完回退逻辑就不再触发：

```bash
curl -X PATCH http://localhost:3002/api/employees \
  -H "Authorization: Bearer <管理员token>" -H "Content-Type: application/json" \
  -d '{"id": 10, "customer_names": ["华夏科技有限公司", "创新品牌管理有限公司"]}'
```

设置页面还没有对应的编辑界面，需要的话我可以加。

## C. 设置页加了「配置可见公司」界面

不用再手敲 curl 了。设置 → 员工管理里，客户角色的账号会多一个 🏢 按钮：

- **表格里直接看到状态**：已配置的显示「可见 N 家：xxx、yyy 等」；未配置的显示橙色警告「未配置可见公司（暂按账号姓名匹配）」，一眼能看出哪些账号还在走回退逻辑
- **弹窗**：从现有订单里自动拉取全部客户公司名做勾选列表，支持搜索；订单里还没出现过的公司名可以在搜索框输入后回车手动添加；已选的以标签形式展示，点击移除
- 弹窗里写明了「一个都不选 = 回退到按账号姓名匹配（同名客户会互相看到）」，避免误操作
- 保存失败会显示后端返回的具体原因，不再静默

`GET /api/employees` 现在会带出客户账号的 `customer_names`，`PATCH` 接受 `customer_names: string[]` 做整表替换。

验证：11 项检查通过（入口、契约、无障碍属性、错误提示、未配置提示），eslint 无警告，`tsc` 零错误。
